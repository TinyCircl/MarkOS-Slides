# Server-side deployed renderer image tag.
# CI/CD overwrites this file on each deployment, and manual compose commands
# should reference it with `docker compose --env-file versions.env ...`.
SLIDEV_VERSION=v0.1
